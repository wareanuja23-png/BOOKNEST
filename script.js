let cart=[];const count=document.getElementById('count'),cartItems=document.getElementById('cartItems'),total=document.getElementById('total');function renderCart(){count.textContent=cart.length;if(!cart.length){cartItems.innerHTML='Your cart is empty.';total.textContent='0';return}let sum=0;cartItems.innerHTML='';cart.forEach((item,index)=>{sum+=item.price;const div=document.createElement('div');div.className='cart-item';div.innerHTML=`<span>${item.name}</span><b>₹${item.price}</b><button class="remove">Remove</button>`;div.querySelector('.remove').onclick=()=>{cart.splice(index,1);renderCart()};cartItems.appendChild(div)});total.textContent=sum}document.querySelectorAll('.add').forEach(button=>{button.onclick=()=>{cart.push({name:button.dataset.name,price:Number(button.dataset.price)});renderCart();button.textContent='✓ Added!';setTimeout(()=>button.textContent='Add to Cart',900)}});const cards=[...document.querySelectorAll('.book-card')];document.querySelectorAll('.category').forEach(button=>{button.onclick=()=>{document.querySelectorAll('.category').forEach(x=>x.classList.remove('active'));button.classList.add('active');const cat=button.dataset.cat;cards.forEach(card=>card.style.display=cat==='all'||card.dataset.cat===cat?'block':'none')}});document.getElementById('search').oninput=e=>{const q=e.target.value.toLowerCase();cards.forEach(card=>card.style.display=card.dataset.name.toLowerCase().includes(q)?'block':'none')};document.getElementById('order').onclick=()=>alert(cart.length?'Thank you! Your order has been placed. We will contact you on 8329805030.':'Please add a book to the cart first.');document.getElementById('contactForm').onsubmit=e=>{e.preventDefault();alert('Thank you! Your message has been sent to BookNest.');e.target.reset()};const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting)entry.target.classList.add('show')}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));renderCart();
